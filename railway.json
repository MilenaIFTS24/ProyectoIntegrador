{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "rootDirectory": "backend"
  },
  "deploy": {
    "startCommand": "rm -rf node_modules && npm install && node index.js"
  }
}
